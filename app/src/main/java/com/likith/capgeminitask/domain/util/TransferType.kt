package com.likith.capgeminitask.domain.util

import com.likith.capgeminitask.R

enum class TransferType(val labelId: Int) {
    DOMESTIC(R.string.domestic),
    INTERNATIONAL((R.string.international))
}